#include "list.h"
#include "item.h"
#include <stdlib.h> 
#include <stdio.h>


Item get_item(Node n) {
    return n->item;
}

/*******************************n***********************************************
 * createNode
 *******************************************************************************
*/
Node list_create_node(Item i) {
    Node n = malloc(sizeof(struct node));
    n->item = i;
    n->next = NULL;
    
    return n;
}

/*******************************************************************************
 * freeNode
 *******************************************************************************
*/
void list_free_node(Node n) {
    /*delete_item(n->item); */
    free(n);
}

/*******************************************************************************
 * insert
 *******************************************************************************
*/
void list_insert(Node head, Node new) {
    new->next = head->next;
    head->next = new;
}


/*******************************************************************************
 * freeAll
 *******************************************************************************
*/
void list_delete(Node head) {
    Node h, aux;
    for (h = head, aux = h; h != NULL; aux = h) {
        h = h->next;
        list_free_node(aux);
    }
}

/*******************************************************************************
 * next
 *******************************************************************************
*/
Node next(Node h) {
    return h->next;
}